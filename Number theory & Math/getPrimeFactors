vector<int> getPrimeFactors(int64_t n) // O(sqrt(n))
{
    vector<int>ret;
    for (int i = 2; i * i <= n; ++i) {
        while (n % i == 0)
        {
            n /= i;
            ret.push_back(i);
        }
    }
    if (n > 1) ret.push_back(n);
    return ret;
}
