int getMex(vector<int>v)
{
    sort(v.begin(), v.end());
    if (v.front() != 0) { return 0; }
    for (int i = 1; i < v.size(); i++)
    {
        if (v[i] - v[i - 1] > 1) { return (v[i - 1] + 1); }
    }
    return (v.back() + 1);
}
