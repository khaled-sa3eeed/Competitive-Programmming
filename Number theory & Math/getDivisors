vector<int64_t> getDivisors(int64_t n)
{
    vector<int64_t> v;
    int64_t i = 1;
    for (; i * i < n; i++)
    {
        if (n % i == 0) { v.insert(v.end(), {i, n / i}); }
    }

    if (i * i == n) { v.push_back(i); }
    sort(v.begin(), v.end());
    return v;
}
